<%= form_for [:dashboard,@consultation], :url => new_dashboard_patient_consultation_path do |f| %>
  <% if @consultation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consultation.errors.count, "error") %> prohibited this consultation from being saved:</h2>

      <ul>
      <% @consultation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <%= f.text_field :doctor_name, :placeholder => 'Doctor' %>
  </div>
  <div class="row">
    <%= f.text_area :description, :placeholder => 'Description / Findings', :rows => 10 %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button button-primary' %>
  </div>
<% end %>
